{
  "name": "native-command-router",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "description": "Compatibility-first native command router with optimization plugins and strict fallback to native tools.",
  "license": "MIT",
  "scripts": {
    "install:local": "bun run scripts/install.ts",
    "uninstall:local": "bun run scripts/uninstall.ts",
    "doctor": "bun run scripts/doctor.ts",
    "bench": "bun run scripts/bench.ts",
    "bench:session": "bun run scripts/bench-session.ts",
    "bench:mcp": "bun run scripts/bench-session.ts",
    "build:runner": "bun build scripts/ncr-runner.ts --target node --format esm --outfile dist/ncr-runner.mjs",
    "build:grep-helper": "bun build scripts/grep-fff-helper.ts --target node --format esm --outfile dist/grep-fff-helper.mjs",
    "build:find-helper": "bun build scripts/fff-find-helper.ts --target node --format esm --outfile dist/fff-find-helper.mjs",
    "plugins": "bun run scripts/plugins-cli.ts",
    "ncr": "bun run scripts/plugins-cli.ts",
    "test:conformance": "bun run tests/conformance.ts",
    "bench:perf": "bun run tests/bench-perf.ts",
    "bench:fast": "bun run tests/bench-fast-profile.ts"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "bun-types": "^1.3.1",
    "typescript": "^5.6.3"
  },
  "engines": {
    "bun": ">=1.1.0"
  },
  "keywords": [
    "cli",
    "router",
    "find",
    "grep",
    "fff",
    "performance",
    "developer-tools",
    "automation",
    "ai-agents"
  ]
}
